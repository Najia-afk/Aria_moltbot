{
  "name": "aria-health",
  "version": "1.1.0",
  "description": "System health checks, self-diagnostics, and auto-recovery for Aria services.",
  "author": "Aria Team",
  "layer": 2,
  "dependencies": [
    "api_client"
  ],
  "focus_affinity": [
    "orchestrator",
    "devsecops"
  ],
  "tools": [
    {
      "name": "check_system",
      "description": "Run health checks on all Aria services and return status summary.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "health_check",
      "description": "Check health of a specific service.",
      "parameters": {
        "type": "object",
        "properties": {
          "service": {
            "type": "string",
            "enum": [
              "database",
              "ollama",
              "litellm",
              "moltbook"
            ],
            "description": "Service to check"
          }
        },
        "required": [
          "service"
        ]
      }
    },
    {
      "name": "get_last_check",
      "description": "Get the result of the last health check.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "get_metrics",
      "description": "Get collected health metrics and statistics.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  ],
  "run": "python3 /app/skills/run_skill.py health {{tool}} '{{args_json}}'"
}
