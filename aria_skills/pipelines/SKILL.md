# pipelines (support folder)

This directory stores YAML pipeline definitions consumed by `aria-pipeline-skill`.

It is a workflow-definition folder, not a standalone skill module.

## Contents
- `*.yaml` files defining multi-step skill pipelines.

## Runtime Owner
- `aria_skills/pipeline_skill/__init__.py`
